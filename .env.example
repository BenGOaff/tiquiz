# ─────────────────────────────────────────────────────────────────
# L'Atelier du Quiz — variables d'environnement
# En DEV local : copie ce fichier en .env.local
# Sur le VPS prod : les vars vivent dans .env (convention serveur Béné,
# cf. AGENTS.md Tiquiz). Source-le avec : set -a; . .env; set +a
# Ne jamais committer .env / .env.local (cf. .gitignore).
# ─────────────────────────────────────────────────────────────────

# Supabase (nouveau projet, séparé de Tiquiz/Tipote)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# URL publique de l'app (sous-domaine Cloudflare).
# NEXT_PUBLIC_APP_URL est inliné au BUILD (valeur figée dans le bundle).
# APP_URL est lu au RUNTIME par le serveur (liens des emails, redirections
# d'auth) : c'est lui qui fait foi pour les liens magiques / d'accès, donc
# modifiable sans rebuild. Mets les deux à la même valeur en prod.
NEXT_PUBLIC_APP_URL=https://quizing.tipote.com
APP_URL=https://quizing.tipote.com

# Webhook Systeme.io (accès après achat)
# - SYSTEME_IO_WEBHOOK_SECRET : secret partagé dans l'URL du webhook (?secret=)
# - SYSTEME_IO_WEBHOOK_SIGNING_SECRET : si défini, on EXIGE la signature HMAC
SYSTEME_IO_WEBHOOK_SECRET=
SYSTEME_IO_WEBHOOK_SIGNING_SECRET=

# Coach IA (API Anthropic). Optionnel au MVP Jour 1.
ANTHROPIC_API_KEY=
# Override optionnel du modèle (sinon défaut centralisé dans lib/anthropicModel.ts)
ANTHROPIC_MODEL=

# Pipeline vidéo auto-hébergé (réutilise l'infra popquiz du VPS : serveur
# tus + Caddy, namespace applicatif "quizing"). Voir infra/VIDEO.md.
# Ces 2 secrets DOIVENT être identiques à ceux ajoutés dans
# /opt/popquiz-tus/.env (côté serveur tus).
QUIZING_JWT_SECRET=
QUIZING_VIDEO_SECRET=
# Endpoint d'upload tus (mount /files). Réutilise l'hôte tus existant.
QUIZING_TUS_ENDPOINT=https://tus.tipote.com/files
# Base de lecture des vidéos servies par Caddy (vhost videos.*).
QUIZING_VIDEO_PLAYBACK_BASE=https://videos.tipote.com
